# Інформаційна вебсистема для створення, перегляду та оцінювання авторських літературних творів

## Опис проєкту

Цей проєкт є бакалаврською роботою, присвяченою розробці інформаційної вебсистеми для роботи з авторськими літературними творами.

Система орієнтована на користувачів, які хочуть переглядати, знаходити, оцінювати та в подальшому публікувати власні літературні твори в межах єдиної вебплатформи.

Проєкт складається з двох основних частин:

- **my-app** — клієнтська частина застосунку;
- **server** — серверна частина та API авторизації.

Архітектура побудована за модульним підходом, що спрощує підтримку, розвиток і документування коду.

---

## Мета проєкту

Метою проєкту є створення зручної та масштабованої вебсистеми для взаємодії авторів, читачів і модераторів, яка забезпечує:

- реєстрацію та авторизацію користувачів;
- перегляд і пошук літературних творів;
- фільтрацію творів за жанром та рейтингом;
- рольову організацію доступу до функцій системи;
- основу для подальшого розширення функціоналу модерації, оцінювання, коментарів і керування публікаціями.

---

## Реалізований функціонал

На поточному етапі в проєкті реалізовано:

- головну сторінку з відображенням списку творів;
- пошук творів за назвою;
- фільтрацію за жанром;
- фільтрацію за мінімальним і максимальним рейтингом;
- сторінку входу;
- сторінку реєстрації з клієнтською валідацією форми;
- збереження стану авторизації через Redux;
- захищений доступ до сторінки особистого кабінету;
- API для реєстрації, входу та отримання даних поточного користувача;
- JWT-авторизацію;
- файлове збереження користувачів у JSON;
- unit-, integration- та UI-тести для окремих модулів системи.

---

## Функціонал, запланований для розвитку

У подальших версіях проєкту передбачається реалізація:

- повноцінної модерації творів;
- завантаження та редагування власних творів;
- коментування та оцінювання;
- вибраного та закладок;
- розширеної адміністративної панелі;
- API-документації та автоматичної генерації технічної документації;
- публікації документації у вигляді окремого вебсайту.

---

## Технологічний стек

### Frontend

- React
- React Router DOM
- Redux Toolkit
- React Redux
- JavaScript
- CSS
- React Testing Library
- Cucumber
- Playwright

### Backend

- Node.js
- Express
- CORS
- bcryptjs
- jsonwebtoken

### Інструменти розробки

- npm
- Git / GitHub
- ESLint
- Jest

---

## Структура проєкту

```text
Diploma/
├── my-app/                  # клієнтська частина застосунку
│   ├── src/
│   │   ├── assets/          # статичні ресурси
│   │   ├── auth/            # захист маршрутів
│   │   ├── components/      # повторно використовувані UI-компоненти
│   │   ├── data/            # локальні тестові дані
│   │   ├── pages/           # сторінки застосунку
│   │   ├── services/        # запити до API
│   │   ├── store/           # Redux store і slice-и
│   │   ├── App.js           # кореневий компонент маршрутизації
│   │   └── index.js         # точка входу frontend
│   ├── features/            # BDD / cucumber feature-файли
│   ├── docs/                # документація frontend-частини
│   └── package.json
│
├── server/                  # серверна частина застосунку
│   ├── routes/              # API-маршрути
│   ├── middlewares/         # middleware
│   ├── utils/               # допоміжні модулі
│   ├── data/                # локальні JSON-дані
│   ├── index.js             # точка входу backend
│   └── package.json
│
├── docs/                    # загальна документація проєкту
├── README.md                # головний опис проєкту
└── LICENSE
```

---

## Архітектурні особливості

### Frontend

Frontend побудований як React-застосунок із поділом на сторінки, компоненти, сервіси та сховище стану.

Основні модулі:

- `App.js` — описує маршрути застосунку;
- `authService.js` — відповідає за HTTP-запити авторизації;
- `authSlice.js` — зберігає стан авторизації та асинхронні дії;
- `ProtectedRoute.js` — обмежує доступ до приватних сторінок;
- `HomePage.js` — реалізує пошук і фільтрацію списку творів;
- `Header.js` і `Footer.js` — формують спільний каркас інтерфейсу;
- `WorkCard.js` та `WorksGrid.js` — відповідають за відображення творів.

### Backend

Backend реалізовано на Node.js та Express.

Основні модулі:

- `index.js` — ініціалізація сервера та підключення маршрутів;
- `auth.routes.js` — маршрути реєстрації, входу та отримання даних користувача;
- `auth.middleware.js` — middleware перевірки JWT;
- `jwt.js` — генерація та перевірка токенів;
- `fileDb.js` — робота з локальним JSON-сховищем користувачів;
- `users.json` — файл для збереження даних користувачів.

---

## Запуск проєкту локально

### 1. Клонування репозиторію

```bash
git clone https://github.com/dimamedvid/Diploma.git
cd Diploma
```

### 2. Запуск frontend

```bash
cd my-app
npm install
npm start
```

Frontend буде доступний за адресою:

```text
http://localhost:3000
```

### 3. Запуск backend

```bash
cd server
npm install
npm run dev
```

Backend буде доступний за адресою:

```text
http://localhost:4000
```

---

## Доступні команди

### Frontend (`my-app`)

```bash
npm start
npm run build
npm test
npm run lint
npm run lint:fix
```

### Backend (`server`)

```bash
npm start
npm run dev
npm test
npm run lint
npm run lint:fix
```

---

## Тестування

У проєкті використовуються різні види тестів.

### Frontend

- unit- та UI-тести через **Jest** і **React Testing Library**;
- BDD-сценарії через **Cucumber**;
- browser/e2e-перевірки через **Playwright**.

### Backend

- unit-тести для middleware;
- тести маршрутів авторизації;
- інтеграційні тести для API.

Тести виконують не лише перевірку працездатності системи, але й виступають прикладами використання окремих компонентів і модулів.

---

## Документування коду

У проєкті використовується підхід документування на основі **JSDoc**.

### Що потрібно документувати

Документуванню підлягають:

- усі публічні функції;
- React-компоненти;
- Redux thunk-и, reducers і slice-и;
- middleware та серверні маршрути;
- утиліти для роботи з токенами, файлами та API;
- складна бізнес-логіка;
- нетривіальні алгоритми фільтрації, валідації або авторизації;
- взаємодія між frontend і backend;
- архітектурні рішення, які важливі для розуміння структури системи.

### Як потрібно документувати

Для кожного нового публічного елемента коду потрібно додавати JSDoc-коментар, який містить:

- короткий опис призначення;
- параметри (`@param`);
- повернене значення (`@returns`);
- можливі помилки (`@throws`), якщо застосовно;
- додаткові пояснення щодо побічних ефектів або особливостей використання.

### Правила для команди

1. Кожна нова функція, компонент або middleware мають бути задокументовані одразу під час розробки.
2. Якщо змінюється логіка функції, документація також повинна бути оновлена.
3. Якщо модуль взаємодіє з API, потрібно описувати формат вхідних і вихідних даних.
4. Якщо код містить складну логіку, крім JSDoc слід додавати короткі пояснювальні коментарі всередині реалізації.
5. Документація має залишатися актуальною після кожного коміту.
6. Усі учасники проєкту повинні дотримуватися єдиного стилю документування.

### Пріоритетні файли для документування

У першу чергу повинні бути задокументовані:

#### Frontend

- `App.js`
- `authService.js`
- `authSlice.js`
- `ProtectedRoute.js`
- `HomePage.js`
- `FiltersBar.js`
- `WorksGrid.js`
- `WorkCard.js`

#### Backend

- `index.js`
- `auth.routes.js`
- `auth.middleware.js`
- `fileDb.js`
- `jwt.js`

---

## Генерація документації

Для автоматичної генерації документації в проєкті використовується **JSDoc**.

Інструкція з генерації документації повинна бути розміщена у файлі:

```text
docs/generate_docs.md
```

У цьому файлі потрібно описати:

- як встановити залежності;
- яку команду потрібно виконати для генерації документації;
- де зберігається згенерований результат;
- як перевірити коректність документації;
- як оновлювати документацію після змін у коді.

---

## Перевірка якості документації

Для підтримки єдиного стилю документування рекомендується використовувати:

- **ESLint** для перевірки стилю коду;
- правила перевірки JSDoc-коментарів;
- регулярне оновлення README та файлів у папці `docs/`;
- тести як приклади правильної поведінки компонентів і API.

---

## API

Серверна частина проєкту містить API для авторизації користувачів.

Поточні основні ендпоінти:

- `POST /api/auth/register`
- `POST /api/auth/login`
- `GET /api/auth/me`
- `GET /api/health`

У подальшому для API планується створення окремої OpenAPI / Swagger документації.

---

## Подальший розвиток документації

Подальший розвиток системи документування включає:

- повне документування всіх публічних інтерфейсів;
- автоматичну генерацію HTML-документації;
- підключення Swagger UI для API;
- використання тестів як “живої” документації;
- публікацію документації через GitHub Pages або інший сервіс;
- налаштування CI/CD для автоматичної генерації документації.
